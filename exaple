from turtle import *
s = Screen()
s.setup(1080, 720)
colors = ['red', 'green']
pencolor('yellow')
pensize(5)
for i in range(5, 0, -1):
    penup()
    setpos(0,-20*i)
    pendown()
    fillcolor(colors[i%2])
    begin_fill()
    circle(20*i)
    end_fill()
mainloop()